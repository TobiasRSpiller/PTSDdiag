% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_combinations.R
\name{apply_symptom_combinations}
\alias{apply_symptom_combinations}
\title{Apply pre-specified symptom combinations to new data}
\usage{
apply_symptom_combinations(data, combinations, n_required = 4, clusters = NULL)
}
\arguments{
\item{data}{A dataframe containing exactly 20 columns with PCL-5 item scores
  (output of \code{\link{rename_ptsd_columns}}). Each symptom should be scored
  on a 0-4 scale where:

\itemize{
  \item 0 = Not at all
  \item 1 = A little bit
  \item 2 = Moderately
  \item 3 = Quite a bit
  \item 4 = Extremely
}}

\item{combinations}{A list of integer vectors. Each vector contains symptom
indices (e.g., \code{c(4, 6, 7, 17, 19, 20)}). All vectors must have the
same length. Typically obtained from \code{results$best_symptoms} of an
optimization run.}

\item{n_required}{Integer specifying how many symptoms must be present
(scored >= 2 on original scale) for a positive diagnosis (default: 4).
Must be between 1 and the length of each combination.}

\item{clusters}{\code{NULL} (default) for non-hierarchical checking, or a
  named list of integer vectors defining the cluster structure for
  hierarchical checking. When provided, a positive diagnosis additionally
  requires that the present symptoms span all defined clusters.

  For PCL-5:
  \code{list(B = 1:5, C = 6:7, D = 8:14, E = 15:20)}}
}
\value{
A dataframe with columns:

\itemize{
  \item \code{PTSD_orig}: Logical. Full DSM-5 diagnosis computed on this data.
  \item One column per combination (logical): Simplified diagnosis for each
    combination. Column names follow the \code{symptom_X_Y_Z} pattern.
}

This dataframe can be passed directly to \code{\link{summarize_ptsd_changes}}
to compute diagnostic accuracy metrics.
}
\description{
Applies one or more pre-specified symptom combinations to a new dataset and
returns a comparison dataframe with the baseline DSM-5 diagnosis and the
simplified diagnosis for each combination. This function enables external
validation by allowing combinations derived from one dataset to be tested
on another.
}
\details{
The function:

\enumerate{
  \item Computes the baseline DSM-5 diagnosis (\code{PTSD_orig}) on the
    provided data using binarized scores
  \item Binarizes the symptom data (scores >= 2 become 1, others become 0)
  \item For each combination, determines diagnosis based on whether at least
    \code{n_required} symptoms are present
  \item If \code{clusters} is provided, additionally checks that present
    symptoms span all defined clusters (hierarchical checking)
  \item Returns a comparison dataframe suitable for
    \code{\link{summarize_ptsd_changes}}
}

Typical workflow for external validation:

\enumerate{
  \item Derive optimal combinations on Dataset A using
    \code{\link{optimize_combinations}} or
    \code{\link{optimize_combinations_clusters}}
  \item Apply those combinations to Dataset B using this function
  \item Compute diagnostic metrics using
    \code{\link{summarize_ptsd_changes}} and
    \code{\link{create_readable_summary}}
}
}
\examples{
# Create example data
set.seed(42)
ptsd_data <- data.frame(matrix(sample(0:4, 400, replace=TRUE), ncol=20))
names(ptsd_data) <- paste0("symptom_", 1:20)

# Define some combinations to test
my_combinations <- list(
  c(1, 6, 8, 10, 15, 19),
  c(2, 7, 9, 11, 16, 20)
)

# Apply combinations (non-hierarchical)
comparison <- apply_symptom_combinations(ptsd_data, my_combinations,
                n_required = 4)

# Compute metrics
metrics <- summarize_ptsd_changes(comparison)
create_readable_summary(metrics)

# Apply with hierarchical checking
pcl5_clusters <- list(B = 1:5, C = 6:7, D = 8:14, E = 15:20)
hier_comparison <- apply_symptom_combinations(ptsd_data, my_combinations,
                     n_required = 4, clusters = pcl5_clusters)
summarize_ptsd_changes(hier_comparison)

}
\seealso{
\code{\link{optimize_combinations}} and
\code{\link{optimize_combinations_clusters}} for deriving optimal
combinations.

\code{\link{summarize_ptsd_changes}} and
\code{\link{create_readable_summary}} for computing and formatting
diagnostic metrics from the returned comparison dataframe.
}
